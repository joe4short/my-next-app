"use client";

import React from "react";

const RegistrationWrapper: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  return <div className="registration-wrapper">{children}</div>;
};

export default RegistrationWrapper;
